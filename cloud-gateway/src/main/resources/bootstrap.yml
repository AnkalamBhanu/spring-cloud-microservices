# This bootstrap will execute before application.yml do # added deps to load bootstrap.yml first
spring:
  cloud:
    config:
      uri:
        - http://localhost:9196
      fail-fast: false           # ← won't crash if config server is down